<!DOCTYPE html>
<html>
<body>

<h1>JavaScript String Methods</h1>
<h2>The bold() Method</h2>

<p id="demo1"></p>

<script>
let text = "According to all known laws of aviation, there is no way that a bee should be able to fly. Its wings are too small to get its fat little body off the ground. The bee, of course, flies anyway because bees don't care what humans think is impossible.";

const words = text.split(" ");
let result = ""; // Initialize an empty result string

// Function to apply bold formatting based on word length
for (let i = 0; i < words.length; i++) {
    let word = words[i];
    let length = word.length; // Get the length of the word
    let bold_length;

    // Determine the bold length based on the length of the word
    if (length >= 9) {
        bold_length = Math.floor(length / 3); // One-third of the length
    } else if (length > 5 && length < 9) {
        bold_length = 3; // First three letters bold
    } else if (length > 3 && length < 6) {
        bold_length = 2; // First two letters bold
    } else {
        bold_length = 1; // First letter bold for words with 3 or fewer letters
    }

    // Create the formatted word
    let formatted_word = "<b>" + word.substring(0, bold_length) + "</b>" + word.substring(bold_length);
    result += formatted_word + " "; // Append the formatted word to the result string
}

// Trim the result to remove any trailing space and set it as innerHTML
document.getElementById("demo1").innerHTML = result.trim();

</script>

</body>
</html>
